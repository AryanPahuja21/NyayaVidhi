import 'package:flutter/material.dart';

class DecisionTreePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Decision Tree Page'),
      ),
      body: const Center(
        child: Text('This is the Decision Tree Page!'),
      ),
    );
  }
}
