import 'package:flutter/material.dart';

class FindLegalAidPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Find Legal Aid Page'),
      ),
      body: Center(
        child: Text('This is the Find Legal Aid Page!'),
      ),
    );
  }
}
